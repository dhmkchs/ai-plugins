---
description: 재현 가능한 조건에서 성능을 재고 원인을 확정한 뒤 고치고 다시 잰다
argument-hint: [측정할 URL]
---

`perf` 스킬을 사용해 진행한다. 스킬의 절차를 건너뛰지 말고 순서대로 따른다.

사용자가 전달한 내용:
$ARGUMENTS

내용이 비어 있으면 스킬을 시작하는 데 필요한 최소 정보를 먼저 묻는다.
