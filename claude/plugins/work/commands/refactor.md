---
description: 얕은 모듈을 깊게 — 삭제 테스트로 판정하고 지표로 확인하는 구조 개선
argument-hint: [대상 모듈·디렉터리 (비우면 git 이력에서 후보 탐색)]
---

`refactor` 스킬을 사용해 진행한다. 스킬의 절차를 건너뛰지 말고 순서대로 따른다.

사용자가 전달한 내용:
$ARGUMENTS

내용이 비어 있으면 git 이력에서 후보를 찾아 제시하고 사용자가 고르게 한다.
